<% content_for :main do %>

  <% if content_for?(:page_header) %>
    <div class="row">
      <div class="span12">
        <%= yield(:page_header) %>
      </div>
    </div>
  <% end %>

  <div class="row">
    <div class="<%= content_for?(:left_column_class) ? yield(:left_column_class) : "span6" %>">
      <%= content_for?(:left_column) ? yield(:left_column) : yield %>
    </div>

    <div class="<%= content_for?(:right_column_class) ? yield(:right_column_class) : "span6" %>">
      <%= yield(:right_column) if content_for?(:right_column) %>
    </div>
  </div>

<% end %>

<%= render :template => 'layouts/curate_nd' %>
